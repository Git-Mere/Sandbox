#pragma once
#include <vector>
#include <iostream>
#include <algorithm>
#include <unordered_set>
#include <queue>
#include <map>


using namespace std;

namespace DP {
	int climbStairs(int n);
	int coinChange(vector<int>& coins, int amount);
}